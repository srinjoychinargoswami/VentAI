import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF6B73FF);        // Purple/Blue
  static const Color background = Color(0xFF0F172A);    // Dark navy
  static const Color surface = Color(0xFF1E293B);       // Lighter dark
  static const Color textPrimary = Color(0xFFFFFFFF);   // White
  static const Color textSecondary = Color(0xFF94A3B8); // Muted gray

  // Chat colors
  static const Color userMessage = Color(0xFF6B73FF);   // Purple
  static const Color aiMessage = Color(0xFF1E293B);     // Dark surface
  static const Color aiText = Color(0xFFe2e8f0);        // Light gray

  // Status colors
  static const Color error = Color(0xFFEF4444);         // Red
  static const Color success = Color(0xFF10B981);       // Green
  static const Color warning = Color(0xFFfbbf24);       // Amber
}
